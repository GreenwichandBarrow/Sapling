# Tech Stack Inventory — Greenwich & Barrow
#
# Validated by Kay 2026-04-24 against her actual stack.
# Read by dashboard/data_sources.py::load_tech_stack().
# Read by Infrastructure page Zone 5 (full inventory reference list).
#
# Each category is rendered as a row of chips. Order here = display order.
# `note` is optional, shows as small muted text after the service name.
# `health` is optional and reflects current operational state:
#   ok      — green dot
#   warn    — yellow dot (e.g., key expiring, schema drift)
#   alert   — red dot (e.g., expired key, broken auth)
#   retired — grey dot (kept for historical reference)
#
# When health probes go live in Zone 2, those override these static values
# for matching services. This file is the canonical "what we run on" list.

categories:
  - label: "AI & LLMs"
    services:
      - { name: "Claude Code", note: "Anthropic · downgraded to individual Pro $17/mo effective Jun 27" }
      - { name: "ChatGPT", note: "OpenAI" }

  - label: "CRM & Pipeline"
    services:
      - { name: "Attio", note: "CRM + future Meet Record" }
      - { name: "Apollo", note: "enrichment only" }
      - { name: "DealsX", note: "email + LinkedIn DMs · summer pause planned", health: warn }

  - label: "Email"
    services:
      - { name: "Gmail", note: "Google Workspace" }

  - label: "Calendar & Scheduling"
    services:
      - { name: "Google Calendar" }
      - { name: "Howie", note: "AI scheduling" }

  - label: "Phone & Voice"
    services:
      - { name: "Google Voice" }

  - label: "Notes & Knowledge"
    services:
      - { name: "Granola", note: "may be absorbed by Attio Meet Record", health: warn }
      - { name: "Obsidian", note: "vault" }
      - { name: "OneNote", note: "Microsoft 365" }

  - label: "Storage & Docs"
    services:
      - { name: "Google Drive" }
      - { name: "Google Docs" }
      - { name: "Google Sheets" }
      - { name: "Google Slides" }
      - { name: "Microsoft Excel", note: "365" }
      - { name: "DocSend", note: "doc sharing + tracking · Personal $15/mo" }


  - label: "Communication"
    services:
      - { name: "Slack" }
      - { name: "LinkedIn" }

  - label: "Design"
    services:
      - { name: "Canva" }

  - label: "Web Presence"
    services:
      - { name: "Squarespace", note: "website" }

  - label: "Operations & Finance"
    services:
      - { name: "Gusto", note: "payroll" }
      - { name: "QuickBooks", note: "accounting · transition with Kick · $41.37/mo" }

  - label: "Development"
    services:
      - { name: "Cursor", note: "IDE" }
      - { name: "GitHub" }
      - { name: "Node.js", note: "runtime" }

  - label: "Infrastructure & Networking"
    services:
      - { name: "Hetzner", note: "server hosting · ~$11/mo" }
      - { name: "Tailscale", note: "VPN / mesh networking · $8.71/mo" }
      - { name: "1Password", note: "password manager · $52.13/yr ($4.34/mo)" }
