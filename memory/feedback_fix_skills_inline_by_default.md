---
name: Fix skills / rules / memories inline by default — don't batch to later
description: Kay's revealed preference over repeated sessions is fix-now. Stop offering "fix now vs defer to Friday calibration." Only batch when mid-high-focus deliverable (owner call prep, investor draft, conference outbound).
type: feedback
originSessionId: 3d944631-1e1f-43d0-88c1-6fdd795901fd
---
When a skill, memory, rule, or stop-hook issue surfaces mid-session, **fix it inline immediately**. Do NOT offer Kay a choice between "fix now" and "batch to Friday / later / calibration hour." Her revealed preference across every prior offer has been fix-now.

**Why:** Kay's calibration is optimized for continuous system improvement — each rule caught and codified same-session prevents 10x future re-catches. Batching to a later slot creates a backlog that (a) she'll have to re-derive context on, (b) risks getting forgotten, (c) adds a decision-fatigue moment ("did I mean to fix that?"). Her framing: the system improves when the correction lands where the error happened. Caught 2026-04-24 after Claude proposed a "Friday calibration hour" to defer skill edits — Kay pushed back: "if you look back I bet you will see whenever you asked before I always answered fix now."

**How to apply:**
- If a rule, memory, skill edit, or hook is warranted, **just fix it and report what landed**. Don't ask permission unless the fix is structurally controversial (e.g., deleting prior codified rules, reshaping a skill's core flow).
- **Exception — narrow:** when Kay is mid-high-focus deliverable (owner call prep, investor deck draft, conference outbound drafts, LOI review), log the issue to an "open fixes" list and surface AFTER the focus task completes. Never break her concentration for rule-promotion.
- Pairs with `feedback_close_out_executes_mutation` (close-outs mutate source-of-truth same session) and `feedback_decision_fatigue_minimization` (don't ask questions already decided).
- Pairs with `feedback_never_reask_decided` — "fix now vs defer" is a decided question, stop asking it.
