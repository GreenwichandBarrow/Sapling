---
name: Command Center dashboard replaces Weekly Activity Tracker
description: Both the Weekly Tracker (Google Sheet) and the Command Center dashboard have Kay as their only audience. Dashboard will subsume the Weekly Tracker — do not treat them as parallel surfaces with different readers.
type: feedback
originSessionId: ebd58436-6958-4e18-98eb-a6797b0d0b46
---
Both the Weekly Activity Tracker (Google Sheet) and the Command Center dashboard have exactly one audience: **Kay**. The dashboard will replace the Weekly Tracker, not coexist with it.

**Why:** 4/24 dashboard build session — I proposed keeping the two separate on "different audiences" grounds (dashboard = Kay, Weekly Sheet = JJ/Guillermo/Jeff). Kay corrected twice: "both audiences are me" and "for efficiency of my time, dashboard will replace." She does not want to maintain two surfaces when one can do the job.

**How to apply:**
- Dashboard roadmap must include a weekly-aggregation view (what the Sheet does today) so the Sheet can be retired.
- Per `feedback_build_new_before_sunset_old.md`: do not retire the Weekly Tracker until the dashboard's weekly view is live and verified.
- When scoping internal ops surfaces in the future, check first whether Kay already has a surface doing the job — prefer merge/replace over parallel.
- External-audience artifacts (investor update, JJ call prep, advisor call preps) are the ones with distinct readers. Internal ops tools default to single-audience (Kay).
